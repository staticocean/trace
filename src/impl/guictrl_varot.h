
// 2021 Softael LLC.
// Created by staticocean on 11/11/2021

// trcctrl - velocity aligned rotation

#ifndef __GUICTRL_VAROT__
#define __GUICTRL_VAROT__

//------------------------------------------------------------------------------

#include <sftlgui/sftlgui.h>
#include <sftltrc/sftltrc.h>

//------------------------------------------------------------------------------

////extern
////void __guictrl_varot_attr__ (void *__ctrl__, void *__attr__)
////{
////    return;
////}
//
//extern
//void __guictrl_varot_edit__ (void *__ctrl__)
//{
//    s_trcctrl_varot *ctrl = (s_trcctrl_varot*) self->data;
////
////    ImGui::Text("desc  ");
////    ImGui::SameLine();
////    ImGui::Text(self->desc);
////
////    ImGui::Dummy(ImVec2(0, 5));
////    ImGui::Separator();
////    ImGui::Dummy(ImVec2(0, 5));
////
////    ImGui::AlignTextToFramePadding();
////    ImGui::Text("ref   ");
////    ImGui::SameLine();
////    ImGui::SetNextItemWidth(ImGui::GetContentRegionAvail().x);
////    gui_objsel("##ref", data->eng->obj_sz, data->eng->obj_ls, &data->ref);
////    if (data->ref != NULL) { data->ref_hash = data->ref->hash; }
////
////    ImGui::AlignTextToFramePadding();
////    ImGui::Text("ellp  ");
////    ImGui::SameLine();
////    ImGui::SetNextItemWidth(-40);
////    gui_ellpsel("##ellp", data->eng->ellp_offset, data->eng->ellp_ls, &data->ellp);
////    ImGui::SameLine(0.0, 0.0);
////    imgui_bool("##ellp_en", ImVec2(-1, 0), &data->ellp_en);
////    if (data->ellp == NULL) { data->ellp_en = 0x00; }
//}
//
////extern
////void __guictrl_varot_view__ (void *__ctrl__)
////{
////    return;
////}

//------------------------------------------------------------------------------

#endif /* __GUICTRL_VAROT__ */
